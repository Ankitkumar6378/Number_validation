function blank(fld)
{
    
    if (fld.value == "") {
        error = "You didn't enter anything \n";
        alert(error);
        return false;


        
    }
    else 
    length(fld);
    
    
}
