function length(fld)
{
     if ( (fld.value.length != 10)) {
      error = "length must be 10 digit!!!!";
      alert(error);
      return false;
     }
      else{
            special(fld);
            
        }
}